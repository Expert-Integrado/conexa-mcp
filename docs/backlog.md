# Backlog — conexa-mcp

1. Validar contra uma instância real do Conexa (rodar `test_connection` e um fluxo de leitura/escrita com Token de Aplicação real) — o desenvolvimento foi feito sem credenciais reais.
2. Acompanhar atualizações da API no Discord "Conexa for Developers"; quando a collection Postman mudar, baixar a versão nova para `spec/postman-collection.json`, rodar `npm run generate` e publicar nova versão.
