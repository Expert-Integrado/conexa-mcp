# Backlog — conexa-mcp

1. Publicar o pacote no npm (`npm publish`) e trocar `github:AsafeSilva/conexa-mcp` por `conexa-mcp` nos exemplos do README.
2. Testar contra uma instância real do Conexa (rodar `test_connection` e um fluxo de leitura/escrita) — o desenvolvimento foi feito sem credenciais reais.
3. Decidir visibilidade do repositório: criado como privado; tornar público antes de publicar no npm.
